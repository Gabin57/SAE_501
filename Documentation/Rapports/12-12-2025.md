# Rapport 12 Décembre 2025

## Développement

### Back-end
- **Réparation API**: Diagnostic et correction du blocage au démarrage (boucle CPU 100%).
- **Corrections de Code**:
  - Correction d'une `SyntaxError` (virgule manquante et accolade en trop) dans `prepare_dataset.py`.
  - Correction d'une erreur de chemin (`Database.images` -> `Database/images`) dans `app.py`.
- **État**: L'API est désormais fonctionnelle et écoute sur le port 5001.

### Infrastructure
- **Réseau**: Résolution du problème "Site inaccessible" via la configuration du pare-feu (`ufw`).
- **Ports**: Ouverture explicite du port 8080 pour l'accès web externe.

### Front-end (Flutter)
- **Compatibilité Web**:
  - Correction du crash lié à l'utilisation de `dart:io` (`File`) dans le navigateur.
  - Implémentation de la détection d'environnement avec `kIsWeb`.
- **Gestion des Images**:
  - Ajout du support des fichiers **SVG** (installation de `flutter_svg`).
  - Optimisation de l'affichage : Les images sont maintenant ajustées (`contain`) avec une marge (`padding`) pour ne plus apparaître "trop grosses".
- **Interface Utilisateur**:
  - Amélioration des cartes : Les titres trop longs sont désormais tronqués proprement avec des points de suspension (...) après 2 lignes.
- **Déploiement**: L'application est redéployée et accessible sur le port 8080.
